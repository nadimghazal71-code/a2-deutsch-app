// The web app's light palette, kept in step so the two feel like one product.
export const colors = {
  bg: '#f5f3ef',
  surface: '#ffffff',
  ink: '#24211c',
  inkSoft: '#6b6560',
  border: '#e4ded4',
  accent: '#b5473a',
  accentInk: '#ffffff',
  accentSoft: '#f1ded9',
  correct: '#2f7a4d',
  incorrect: '#b5473a',
};

// Level 0 (never seen) through 5 (mastered). Used by the progress bars and by
// the coloured border that tells you how well you knew a card last time.
export const levelColors = [
  '#d9d4cb',
  '#e8b4a8',
  '#eecf8f',
  '#d7d98a',
  '#9fc98a',
  '#5a9e6f',
];

export const radius = { card: 16, control: 12, pill: 999 };
